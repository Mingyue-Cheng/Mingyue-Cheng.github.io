# Cross-page consistency

## Scope

Use the latest user-supplied English research introduction on the homepage and Research page. Translate the same meaning into Chinese, preserving LLM-driven reasoning and agents as the core and time-series/science intelligence as application drivers. Align the short profile statement and research metadata with this positioning.

Synchronize duplicated research directions, application cards, research links, and recruitment copy in both languages. Keep the Prediction Intelligence detail page and its specialized framework intact. Preserve the exact supplied English introduction, including its original wording.

Organize the complete Open Source page using the homepage's three categories; retain all existing projects and datasets. Shared projects retain identical descriptions and links. Complete the corresponding language hooks, along with publication filter labels and common navigation/footer labels.

Use the homepage's approved Prussian-blue, left-accent application-card styling on both research surfaces. Retain justified prose, responsive stacking, focus states, reduced-motion support, and existing page containers. Unify standard subpage hero spacing and typography without altering the dedicated Prediction Intelligence layout.

## Boundaries

Do not change publication identities/order, news events, grant records, personal details, external resources, or unrelated local files. Keep this update local: no commit, push, or deployment is requested.

## Verification

Add cross-page tests that fail against the current drift, including fallback HTML and language-switch behavior. Run the full existing test suite, JavaScript syntax checks, and diff whitespace checks. Use an isolated browser test session for desktop/mobile and language-switch checks if the bundled browser runtime is available.
